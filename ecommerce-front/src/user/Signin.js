import React from 'react';
import Layout from '../core/Layout';

const Signin = () => {
    return (
        <Layout title="LOGIN" description="Sign in">
            Signin
        </Layout>
    )
};

export default Signin;
